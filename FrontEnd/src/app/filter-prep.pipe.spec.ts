import { FilterPrepPipe } from './filter-prep.pipe';

describe('FilterPrepPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrepPipe();
    expect(pipe).toBeTruthy();
  });
});
